{
  "name": "wobble-heads",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "start": "python3 -m http.server 8080",
    "benchmark:geometry": "node scripts/benchmark-geometry.mjs"
  },
  "devDependencies": {
    "three": "0.170.0"
  }
}
